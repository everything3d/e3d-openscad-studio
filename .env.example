# Postgres connection string.
# Local dev: any Postgres works, e.g. postgres://<user>@localhost:5432/e3d_openscad_studio
# On Vercel: provisioned automatically when you add Neon (or another Postgres) from the Marketplace.
POSTGRES_URL=

# OpenRouter API key (https://openrouter.ai/settings/keys).
# Required in every environment, including on Vercel.
OPENROUTER_API_KEY=

# Optional: override the agent model (default: openai/gpt-5.6-terra).
# Any model id from https://openrouter.ai/models — must support tool calling
# and image input (users attach reference photos and sketches).
# STUDIO_MODEL=anthropic/claude-sonnet-4.5

# Optional: comma-separated fallback chain. If the model above is unavailable
# (unknown id, rate-limited, upstream provider down) OpenRouter tries these in
# order within the same request.
# STUDIO_MODEL_FALLBACKS=anthropic/claude-sonnet-4.5,google/gemini-2.5-pro

# Optional: override the model that auto-names projects (default: openai/gpt-5.4-nano).
# NAMING_MODEL=google/gemini-2.5-flash-lite
# NAMING_MODEL_FALLBACKS=

# Optional: the URL reported to OpenRouter for app attribution on its dashboard.
# OPENROUTER_APP_URL=https://your-deployment.vercel.app

# Optional: model used to draft reusable starter metadata.
# STARTER_DRAFT_MODEL=openai/gpt-5.4-nano
# STARTER_DRAFT_MODEL_FALLBACKS=

# Comma-separated Clerk user IDs allowed to publish starters to every signed-in user.
# All users can still create private starters for themselves.
# CANONICAL_PUBLISHER_USER_IDS=user_abc123

# Clerk (auto-provisioned when added via Vercel Marketplace: vercel integration add clerk)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
